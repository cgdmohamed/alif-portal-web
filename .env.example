# The Alef Future API's URL (not this portal's own URL) — where the
# dashboard sends its HTTP requests. Baked into the JS bundle at build
# time; see docker-compose.yaml's build.args for the production equivalent.
VITE_API_URL=https://api.aliffuture.com
